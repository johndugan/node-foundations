// Here we assign an object literal that contains
// and `events` object who stores the name of
// events that we use throughout our node project.

// This is a best practice and makes debugging
// easier.
module.exports = {
    events: {
        // key: 'value'
        GREET: 'greet'
    }
};